image: golang:1.25

pipelines:
  default:
    - step:
        name: Build, vet, and test
        caches:
          - go
        script:
          - go build ./...
          - go vet ./...
          - go test ./...
